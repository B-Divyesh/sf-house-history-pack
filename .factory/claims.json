[
  {
    "id": "demo-isolated",
    "claim": "Try it with sample data opens a separate, disposable sample record.",
    "where": "landing action and demo banner",
    "test": "npm run test:e2e -- --grep @claim:demo-isolated",
    "sandbox": "fresh browser context; click the landing action; assert the demo IndexedDB namespace, seeded record, and reset behavior"
  },
  {
    "id": "offline-reload",
    "claim": "The sample record works offline after the first visit.",
    "where": "README and offline state",
    "test": "npm run test:e2e -- --grep @claim:offline-reload",
    "sandbox": "fresh browser context; load /demo, wait for the service worker, set offline, and reload"
  },
  {
    "id": "local-only",
    "claim": "Records are not sent off this origin during the sample workflow.",
    "where": "Private by default note and pack builder",
    "test": "npm run test:e2e -- --grep @claim:local-only",
    "sandbox": "fresh browser context at /demo; capture every browser request while opening the pack builder"
  },
  {
    "id": "portable-exports",
    "claim": "The sample record creates a PDF and a ZIP with its evidence.",
    "where": "Build pack controls and README",
    "test": "npm run test:e2e -- --grep @claim:portable-exports",
    "sandbox": "fresh browser context at /demo; download both formats and inspect the ZIP entries"
  },
  {
    "id": "encrypted-backup",
    "claim": "A password-protected full backup can be downloaded.",
    "where": "encrypted backup control and README",
    "test": "npm run test:e2e -- --grep @claim:encrypted-backup",
    "sandbox": "fresh browser context at /demo; create a backup and inspect its encrypted PBKDF2/AES-GCM envelope"
  },
  {
    "id": "pack-plus-price",
    "claim": "Pack Plus is a one-time $29 purchase and core exports remain free.",
    "where": "landing facts and Pack Plus panel",
    "test": "npm run test:e2e -- --grep @claim:pack-plus-price",
    "sandbox": "fresh browser context at /demo; assert the displayed price and enabled free export controls"
  }
]
